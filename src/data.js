/* Manejo de data */

// esta es una función de ejemplo
// puedes ver como agregamos la función a nuestro objeto global window
/*
aqui van las funciones
function filterData (data, condition) en archivo src/data.js filtra
sortData(data, sortBy, sortOrder) en archivo src/data.js ordena
computeStats(data) en archivo src/data.js esto permite hacer cálculos estadísticos básicos
*/
const filterData = () => {
  // return 'example';
};

const sortData = () => {
  
}
const computeStats = () => {

}

window.filterData = filterData;
window.sortData = sortData;
window.computeStats = computeStats;
